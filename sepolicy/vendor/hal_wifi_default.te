# WLAN and Tether
allow hal_wifi_default exported_wifi_prop:property_service set;
allow hal_wifi_default proc_net:file { read write};
allow hal_wifi_ext wifihal_socket:dir { search read write add_name };
allow hal_wifi_ext vendor_tombstone_data_file:dir { search read };
set_prop(cnd, wifi_prop)
